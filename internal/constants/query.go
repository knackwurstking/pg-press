// Package constants provides shared constants for the application.
package constants

// Query parameter names
const (
	// QueryParamID is the query parameter name for ID values
	QueryParamID = "id"

	// QueryParamClose is the query parameter name for close actions
	QueryParamClose = "close"

	// QueryParamTime is the query parameter name for time values
	QueryParamTime = "time"

	// QueryParamPress is the query parameter name for press values
	QueryParamPress = "press"

	// QueryParamAttachmentID is the query parameter name for attachment IDs
	QueryParamAttachmentID = "attachment_id"
)
