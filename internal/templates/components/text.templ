package components

templ NotFoundText(text string) {
	<p class="text-muted-foreground w-full text-center italic text-sm p-2">
		if text != "" {
			{ text }
		} else {
			Der angeforderte Inhalt wurde nicht gefunden.
		}
	</p>
}
