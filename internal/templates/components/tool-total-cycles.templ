package components

import "fmt"

// ToolTotalCycles renders an input field displaying the total cycles since last regeneration
templ ToolTotalCycles(totalCycles int64) {
	<input
		type="number"
		value={ fmt.Sprintf("%d", totalCycles) }
		readonly
		class="total-cycles-display"
		title="Gesamtzyklen seit der letzten Regenerierung"
	/>
}
